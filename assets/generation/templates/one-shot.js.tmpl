#!/usr/bin/env node
"use strict";

// Place this entry inside generated/__COMMAND__/ and declare it in metadata.toml.
// Accept no command-line input and do not prompt.
// Resolve relative paths from the process current working directory.
// Keep the package self-contained and never embed credentials or absolute user paths.
// Use Node.js when it simplifies the task compared with available system tools.
// Write only the required logic; add functions only when needed.
