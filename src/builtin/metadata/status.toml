name = "status"
category = "system"
description = "SCV source와 현재 activation 상태를 비교합니다."
usage = "scv status [--json]"
builtin = true
runtime = "builtin"
platforms = ["windows", "linux", "macos"]
risk = "read"
network = false
supports_dry_run = false
effects = ["reads and validates the SCV source and current activation"]

[[options]]
long = "--json"
description = "상태를 JSON으로 출력"

[[examples]]
command = "scv status --json"
