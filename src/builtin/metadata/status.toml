name = "status"
category = "system"
description = "Compare the SCV source with the current activation."
usage = "scv status [--json]"
builtin = true
runtime = "builtin"
platforms = ["windows", "linux", "macos"]
risk = "read"
network = false
supports_dry_run = false
effects = ["reads and validates the SCV source and current activation"]

[[options]]
long = "--json"
description = "Print status as JSON"

[[examples]]
command = "scv status --json"
